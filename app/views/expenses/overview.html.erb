<p id="notice"><%= notice %></p>

<div class="section">
  <div class="speech-bubble-bottom">
    <p>This is where you can manage your personal expenses.</p>
    <p>On my left is the overview of all your expenses, grouped by months and categories respectively.</p>
    <p>Before you can record an expense, you will have to first create the item in its respective category.</p>
  </div>
    <img src="/assets/butler/butler2.png" alt="butler" title="Mr. Butlter"/>
</div>

<div class="two-third">
  <h1>Expenses Overview</h1>
  <p><%= link_to "+ Record New Expenses", new_profile_expense_path(@profile) %></p>
  <p><%= link_to "View All Expenses", profile_expenses_path(@profile) %></p>
  <p><%= link_to "View All Categories", profile_categories_path(@profile) %></p>

  <div class="clearfix"></div>
  
  <div class = "half">
    <table>
      <thead>
        <tr>
          <th>Month</th>
          <th>Amount</th>
        </tr>
      </thead>
      <tbody>
        <% @monthly_exp.each do |month, expenses| %>
          <tr>
            <td><%= month %></td>
            <% count_array = expenses.collect { |e| e.amt } %>
            <td><%= count_array.sum %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
  <div class"half">
    <table>
      <thead>
        <tr>
          <th>Category</th>
          <th>Amount</th>
        </tr>
      </thead>
      <tbody>
        <% @cat_exp.each do |cat, expenses| %>
          <tr>
            <td><%= cat %></td>
            <% count_array = expenses.collect { |e| e.amt } %>
            <td><%= count_array.sum %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>