<%= form_for([@group, @activity, @schedule]) do |f| %>
  <% if @schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% @schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :day %><br>
    <%= f.text_field :day %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.time_select :start_time %>
  </div>
  </div>
  <div class="field">
    <%= f.label 'Person in charge' %><br>
    <%= f.select :membership_id, Membership.where(:group_id => @group).collect { |m| [Profile.find(m.profile_id).fname, m.id]  } %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
