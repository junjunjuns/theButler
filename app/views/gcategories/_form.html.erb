<%= form_for([@group, @gcategory]) do |f| %>
  <% if @gcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gcategory.errors.count, "error") %> prohibited this gcategory from being saved:</h2>

      <ul>
      <% @gcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :desc %><br>
    <%= f.text_field :desc %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
